// enum - enumeration
// difference between normal objects and enum
// normal objects can used for everything to store methods for different purpose
// where enum are closely watched values that means even if these values aren't used here they are important
export enum MatchResult {
  HomeWin = 'H',
  AwayWin = 'A',
  Draw = 'D'
};